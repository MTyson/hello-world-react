version: 2
jobs:
  build:
    docker:
      - image: debian:stretch

    steps:
      - checkout

      - run:
          name: install
          command: npm i

      - run:
          name: run tests
          command: echo run test

      - run:
          name: run lint
          command: echo run lint
